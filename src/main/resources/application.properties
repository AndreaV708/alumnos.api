# Configuración para PostgreSQL en Render
# Usar variables de entorno en producción
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d4qaqqili9vc739qs5t0-a.oregon-postgres.render.com:5432/soaproject}
spring.datasource.username=${DATABASE_USERNAME:soauser}
spring.datasource.password=${DATABASE_PASSWORD:7fvacRwnNV9fdJWYJtOpuavVHSyLTqE1}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server port (Render asignará automáticamente en producción)
server.port=${PORT:8081}